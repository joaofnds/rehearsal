---
id: doc-43
title: triage-2026-09-08-d
type: other
created_date: '2026-09-08 16:51'
---

# Triage, 2026-09-08 (d)

Thirteenth triage on this board. Previous run: doc-41 (2026-09-08 12:27),
about four and a half hours earlier. Goal (doc-6/doc-7, unchanged): prove the
loop once (m-1), then the UI three (m-5, m-6, m-7), then m-2, then m-3.

## What changed since doc-41

One reflection doc landed since doc-41 and outranks it: doc-42 (reflection on
ACT-51, 2026-09-08 16:17). It found ACT-51's own final summary falsely
claimed "closing ACT-26.7" (11 of 17 console.log sites in src/benchmark/
still write to stdout, verified by direct grep; ACT-26.7 itself is 0/6). The
same session corrected the claim on ACT-51's own record in commit 56534ba,
before this run started. Nothing further to do there; verified the grep
still returns 11 matches and ACT-26.7 is still 0/6, untouched.

ACT-51 closed to Done since doc-41 (all six of its own acceptance criteria
checked against a real `--model sonnet` run, a real `kill -9` and restart).
Two follow-on cards were filed during its build and review: ACT-121
(`probeModelAvailable` conflates budget exhaustion with model rejection) and
ACT-122 (`redactAbsolutePaths` misses Node's quoted-path shape). Both
verified this run: ACT-121's cited mechanism (terminal_reason parsed only via
a `.loose()` catch-all) checks out in claude.ts; ACT-122's reproduction was
re-run directly and reproduces exactly as recorded. m-6 sits at 1/2, matching
`backlog milestone`.

Open set: 40 (39 To Do, 1 Shape), up from 32 at doc-41 net of ACT-35 closing
and ACT-51 closing, ACT-119/ACT-121/ACT-122 opening.

## Verified this run

Read every open card in full (all 40) and checked every claim on each that a
grep, a file read, or a command could settle. Delegated a second independent
pass over 37 of the 40 (all but ACT-51, ACT-61, ACT-121, ACT-122, which I
checked myself) to a reviewer sub-agent, then verified its six findings
directly rather than relaying them:

- **ACT-117**: confirmed done, AC#2. `MODEL_PROBE_BUDGET_USD` reads `0.1` in
  `src/benchmark/preflight.ts:62` (commit a98bcc7, same day as filing). AC#1
  (telling `budget_exhausted` apart from a genuine rejection) is still open;
  `readClaudeEnvelope` still throws a bare `Error` regardless of cause. Noted
  on the card.
- **ACT-27 / ACT-31**: confirmed their shared "no recorded run" premise is
  stale. `rehearsal.ts list runs` shows a real recorded run
  (`run:2026-09-06T21-58-29.508Z audit-log STOPPED:build replayable`), and
  `.benchmark-runs/` holds comparisons, replays, sessions, and a
  run-events.sqlite. Both cards were kept Low specifically because nothing
  observable existed yet; that basis no longer holds. Noted on both cards.
  Priority change is not mine to make (see Questions).
- **ACT-96**: citation error confirmed. `HashedFile` is at
  `src/benchmark/checkpoint.ts:13`, not `corpus-file.ts:14` as cited. The
  substantive claim (no role field on it) still holds. Noted on the card.
- **ACT-95**: confirmed the framing undersells what exists.
  `deriveStaleness` (checkpoint.ts:479-482) already compares
  `settingsFile.sha256` and already adds a cause on mismatch; two of three
  call sites already wire it. `staleness-report.ts` has zero mentions of
  `settingsFile` anywhere in the file, confirming `staleCheckpoints` never
  populates the field `rehearsal stale` actually reads. Narrowed the fix
  target on the card to that one call site.
- **ACT-60**: confirmed a real stale premise, not just drift. `~/.agents/`
  no longer contains `rules/`; it now holds `rulebook/` (with
  `engineering-judgment.md`, `coding-style.md`, etc., matching what this
  project's own CLAUDE.md already routes to). The card's every acceptance
  criterion names `rules/` specifically. The underlying gap (a corpus A/B
  changing an instruction file the case never declares measures nothing) is
  unaffected, but the fix must be re-scoped against what a session actually
  loads today before it is built. Noted on the card.
- **ACT-84**: version-number drift only (bun now 1.4.2, cited 1.4.1); no
  substance change, noted for completeness.

No other card's claims were found stale. No duplicates found beyond what
cards already cross-reference themselves (ACT-86/ACT-115 sequential, not
duplicate; ACT-92/ACT-93 explicitly related-but-distinct, both already say
so). No card is fully satisfied by existing code without being closed,
beyond ACT-117's AC#2 above.

Confirmed no card is in Build or Review, and no session holds anything.
`git status` was clean before and after the seven card edits this run made
(ACT-60, ACT-95, ACT-96, ACT-117, ACT-27, ACT-31, ACT-84), each an
`--append-notes` addition; no acceptance criteria, priority, status, or
description text was rewritten.

## Classification

No overtaken or duplicate card this run. ACT-121/ACT-122 (filed since doc-41)
are each a new, independent finding, not restatements of anything open or
Done.

## Priorities

No priority set or changed this run. ACT-27 and ACT-31's stale "nothing
observable yet" basis for staying Low is flagged above and in Questions
rather than acted on, since priority between features is not triage's to
decide.

## The queue

`backlog task list --ready --sort priority` plus the sitting bundles.
Unchanged in shape from doc-41 except ACT-119 landing in the Medium tier
(already done at doc-41) and the corrections above, which change no
priority.

1. **ACT-61** (Shape, Medium): unblocked and answered since 2026-09-07, still
   nothing built. Seventh consecutive run flagging it (see Questions).
2. **ACT-114** (m-7, Medium): closes m-7's own named risk, still unproven
   against a real recorded comparison.
3. **ACT-60** (m-3, Medium): re-scope against `rulebook/` before building,
   per this run's finding.
4. **ACT-71, ACT-84** (independent, Medium/unset).
5. **ACT-69** (blocked on João's budget number).
6. **ACT-27, ACT-31** (Low, but see Questions: their deprioritizing premise
   is now false).
7. **ACT-90, ACT-92, ACT-93** (Low, independent; ACT-92/93 can run
   together).
8. **ACT-95, ACT-113, ACT-116, ACT-117 (AC#1 only now), ACT-118, ACT-119**
   (Medium, independent of each other and the rest).
9. **ACT-115** (Medium): unblocks ACT-86's own pending mechanism.
10. **ACT-106** (independent, unblocked, right-sized; no milestone).
11. **ACT-96-105, ACT-109, ACT-112** (independent backend gaps; ACT-96's
    citation is now fixed).
12. **ACT-86** (blocked on ACT-115 landing, then on observing a future
    session's card write).
13. **ACT-83** (parked by direction; do not pick up from the ready queue
    despite showing as ready — the CLI has no "parked pending milestone"
    state, so this recurs every run until built or the CLI models it).
14. **ACT-120** (bug, real cost evidence: ~1.5+ USD burned twice in one
    iterate run from column-routing alone; independent, unblocked).
15. **ACT-121, ACT-122** (new this run, independent, unblocked).

Bundles that cannot run in parallel: unchanged from doc-41 (ACT-60/ACT-118
if ACT-60 touches the manifest-probe fixture; ACT-26.7 before ACT-27/ACT-31;
ACT-114's check and ACT-109's schema change touch the same comparison-record
path).

## Kaizen candidates

**Escalating.** Doc-42's finding (ACT-51's own final summary asserted
ACT-26.7's closure without checking ACT-26.7's own acceptance criteria) is
the same class of defect named at doc-28 (ACT-86), doc-32 (ACT-48), and
doc-33/doc-36/doc-38 (carried since, never started): a card's own record
states a fact about another card, or about itself, that direct inspection
contradicts, and nothing prompted the checkable verification before it was
written down. This is now a fourth independent instance across four
different sessions and three different kinds of work (a chore card's
completion claim, a shape session's decision-recording, a build card's
cross-card closure claim). The corpus's own "Claims" rule already covers
this exactly; the gap is procedural, not doctrinal. Recommending kaizen by
name now rather than carrying it to a fifth run.

The other two carried candidates (three m-1 reliability cards closed on
inference rather than a live pipeline run; the iteration guard firing on a
session's own assignee bookkeeping) are unchanged and still not started.

ACT-120's own card (filed this cycle, not by triage) already documents a
third process defect in the same iterate run doc-42 reviewed: a card whose
stage finished with work still owed stayed in its old column twice, burning
a session each time to rediscover that. This is a different failure
(routing, not claims) and the card already self-widened to cover both
observed instances with acceptance criteria. Not adding it to kaizen
separately since it is already a well-formed, sized build card; naming it
here only so a future run doesn't file it a second time.

## Questions to João

1. **ACT-61**: unblocked and answered since 2026-09-07, still sitting in
   Shape with nothing built, now a seventh run flagging it. Recommending:
   move to To Do.
2. **ACT-27 / ACT-31**: both were deliberately kept Low on the stated
   premise that the tool had never run its own pipeline, so restructuring
   changed nothing observable. That premise is now false: a real run is
   recorded and replayable. Recommending: reconsider their priority now that
   the condition they were waiting on has happened. Not raising this as a
   priority call myself, since priority between features is not triage's
   to decide.
3. **Kaizen**: recommending it start now on the four-instance
   claims-without-verification pattern above, named by kind rather than
   waiting for a fifth.
