---
id: doc-38
title: triage-2026-09-08
type: other
created_date: '2026-09-08 10:45'
---

# Triage, 2026-09-08

Tenth triage on this board. Previous run: doc-36 (2026-09-07 22:29), about 12
hours earlier. Goal (doc-6/doc-7, unchanged): prove the loop once (m-1), then
the UI three (m-5, m-6, m-7), then m-2, then m-3.

## What changed since doc-36

ACT-50 closed to Done (m-7's sole card): both the comparison screen and the
corpus screen shipped, two review rounds, four defects found and fixed by a
live browser check the test suite could not reach. Its reflection (doc-37,
2026-09-08 01:45) found the card's own named risk landed: no real comparison
record exists on this checkout, so the comparison screen has only ever been
proven against fixtures and the not-found path. That reflection proposed one
follow-up card, filed the same session as **ACT-114** (commit 8d77ff4,
already correctly scoped: get a real comparison in front of the screen and
settle why `.benchmark-runs/comparisons` is empty despite ACT-39 being Done).
Also filed since doc-36, all from ACT-50's own build and review: **ACT-109**
(no per-rep grade in a persisted comparison, so the design's per-pair table
has no rows), **ACT-112** (attribution's silent first-hash-wins when an arm's
own corpus is internally inconsistent across stages), **ACT-113** (Medium;
hashDirectory follows symlinks, a real containment gap a planted symlink
proved live). No card closed besides ACT-50. Open set: 29 before, 33 now
(net +4: ACT-109, ACT-112, ACT-113, ACT-114 filed; ACT-50 closed).

## Verified this run

Read doc-36 and doc-37 in full, and the commit log since doc-36's timestamp
(72 commits, all ACT-50 build/review/close plus the doc-37 reflection and
ACT-114 filing; nothing else landed). Cross-checked ACT-114, ACT-109,
ACT-112, ACT-113 against doc-37 and against ACT-50's own review notes: each
names a distinct gap, none overlaps another, all four are scoped exactly as
their filing sessions described. ACT-104's Medium priority (recommended in
doc-36) is set. ACT-38 (Done, 2/4 boxes checked) already carries its own
accounting for both unchecked boxes on the card, one voided as a wrong
criterion, one filed as ACT-44; no action needed.

Re-checked ACT-86, carried in doc-36 as unresolved. Doc-33's note called
the claimed fix (`--append-notes` named in the shape/build skills) false,
citing a commit `e33c7abf` that "does not exist." That premise was itself
wrong: the commit exists in `~/code/dotfiles` (2026-09-06) and did add the
line to both skills. But the line is absent today, in both the dotfiles
source and the chezmoi-rendered `~/.agents/skills/{shape,build}/SKILL.md`,
because a later commit, `30645173` (2026-09-07 19:16, "correct a probed
claim, then revert the instruction edits that overreached"), reverted it as
collateral in an unrelated batch revert. Net state is unchanged from doc-36
(criterion #1 not in force), but the reason was wrong on the card; corrected
directly on ACT-86 with the dated finding. Fixing dotfiles is outside this
board's scope, so the card stays open, unreassigned.

All twelve of the queue's top unblocked cards (ACT-59, ACT-60, ACT-51,
ACT-35, ACT-84, ACT-69, ACT-90, ACT-92, ACT-93, ACT-95, ACT-27, ACT-31) carry
an `Updated` timestamp from before doc-36 or from routine priority-setting on
2026-09-07 16:24; none changed since and none carries a new stale claim.

## Classification

No overtaken or duplicate card found. ACT-109, ACT-112, ACT-113, ACT-114 are
each their own instance, not restatements of ACT-96-105's ten gaps or of each
other. ACT-86's stale premise is the only correction this run made (see
above).

## Priorities

No change beyond what doc-36 already set. ACT-109, ACT-112, ACT-113, ACT-114
arrived unprioritized except ACT-113 (Medium, set by its filer, matching a
live-proven security gap). Recommending Medium for ACT-114 specifically: it
is the direct closer of m-7's own named risk (the last milestone card in the
UI-three sequence has never been checked against real data) and is cheap
relative to that payoff, the same reasoning doc-36 used for ACT-104.
Recommending Medium for ACT-109 too: it blocks the design's per-pair table
from ever having rows, which is the concrete content ACT-114's own check
will be looking at. Leaving ACT-112 unprioritized: it names a product
decision this triage cannot make (refuse the attribution claim, warn, or
something else), not a sized build.

## The queue

`backlog task list --ready --sort priority`, ordered against the live goal.
Unchanged in shape from doc-36 except for the four new arrivals, which sit
alongside ACT-96-106 as backend gaps pending a UI session's pull.

1. **ACT-114** (m-7, recommending Medium): closes the one thing standing
   between "m-7 shipped" and "m-7 proven." Needs either a live `rehearsal
   compare` run or locating ACT-39's original comparison output.
2. **ACT-35** (m-1-adjacent, Medium, ~7 USD, already approved).
3. **ACT-59** (m-3, High) **→ ACT-60** (m-3, Medium).
4. **ACT-71, ACT-84** (independent, Medium/unset).
5. **ACT-69** (blocked on João's budget number).
6. **ACT-90, ACT-92, ACT-93** (Low, independent; ACT-92/93 can run together).
7. **ACT-95, ACT-113** (Medium, independent of each other and the rest).
8. **ACT-106** (independent, unblocked, right-sized; no milestone).
9. **ACT-96-105, ACT-109, ACT-112** (independent backend gaps; pick up
   alongside whichever UI screen needs the data, or, for ACT-112, once a
   product call is made).
10. **ACT-86** (still blocked on itself: prose fix reverted, hook ruled out
    by João; no unstarted mechanism left to try without new direction).
11. **ACT-83** (parked).
12. **ACT-27** (Low) **→ ACT-31** (Low), still behind **ACT-26.7** (Low) for
    file overlap.

Bundles that cannot run in parallel: ACT-26.7 before ACT-27/ACT-31 (same
files); ACT-59 before ACT-60 (dependency); ACT-114's check and ACT-109's
schema change touch the same comparison-record path and should not run at
the same time as each other.

ACT-61 stays excluded from ready: it moved from "open question" to
"answered, unblocked" between doc-33 and doc-36 (explicit declaration,
recorded on the card 2026-09-07), but is still sitting in Shape, not To Do,
with 0/4 acceptance criteria checked. This is the fourth run in a row this
card has been read and left untouched; flagging as a question below rather
than moving it myself, since moving Shape-to-To-Do on someone else's card is
a judgment call about whether shaping is actually finished, not a mechanical
correction.

## Kaizen candidates

No new instance this run beyond ACT-86's corrected-premise finding, which is
itself the same class as the three already carried in doc-33/doc-36 (a
card's own notes asserting a fact that direct inspection contradicts): now a
third occurrence, and the second one specifically on ACT-86's own notes
about itself (the note claiming the fix landed was wrong in doc-33's telling,
then doc-33's own correction of that claim was also wrong in a different
way). The other two carried candidates (three m-1 reliability cards closed
on inference rather than a live pipeline run; the iteration guard firing on
a session's own assignee bookkeeping) are unchanged and still not started.

## Questions to João

1. **ACT-61**: unblocked and answered since doc-36, still sitting in Shape
   with nothing built. Move it to To Do now, or is there a reason it's
   staying in Shape that isn't recorded on the card? Recommending: move to
   To Do, Medium priority already set.
2. **ACT-86**: two rounds of prose fix have now failed to stick (once never
   loaded, once loaded then reverted as collateral), and the one stronger
   mechanism (a PreToolUse hook) was ruled out. Is there a third mechanism
   worth trying, or does this stay open indefinitely as a known, accepted
   gap? Recommending: re-land the reverted line as its own isolated commit
   (not bundled with other instruction edits) so a future revert of
   unrelated overreach doesn't take it out again, but this is a judgment
   call about whether that's worth a card of its own or just a one-line
   fix the next session passing through those files makes.
