---
id: doc-16
title: triage-2026-09-05-b
type: other
created_date: '2026-09-05 21:22'
---

# Triage, 2026-09-05 (second pass)

Fourth triage on this board. The previous run is doc-12 (labeled 2026-09-05,
timestamped 2026-09-04 23:01; a title/date mismatch in that file, not fixed
here since it changes nothing). Newest reflections read first: doc-13
(ACT-70), doc-14 (ACT-44), doc-15 (ACT-45). None changes the plan; doc-14
found ACT-39 and ACT-43 Done where an earlier draft had called them open, and
the board confirms both closed.

## What changed since doc-12

The goal is doc-7's milestone order, unchanged: m-1 (prove the loop once)
first, then m-5/m-6/m-7 (UI), then m-2, then m-3.

**m-1's own gate closed.** ACT-38 and ACT-39 are both Done. The loop the
project exists to prove has now run for real: a checkpoint was replayed
against an edited instruction and the two attempts were compared. ACT-41,
ACT-44, ACT-45, ACT-63, ACT-64, ACT-65 are Done too. This is the central
fact of this run: the walking skeleton doc-6 called "the only work that
tests the project's premise" has now walked once.

Closed since doc-12: ACT-39, ACT-41 (already Done at doc-12), ACT-44,
ACT-45, ACT-63, ACT-64, ACT-65, ACT-70, ACT-72, ACT-75, ACT-77, ACT-78,
ACT-79, ACT-80, ACT-81, ACT-82. ACT-42 is not closed; it is still open.

Created since doc-12, all found live while running ACT-39's own replay or
its follow-on work: ACT-69, ACT-71, ACT-73, ACT-74, ACT-76 (comparison and
corpus-recording gaps found running the real loop), ACT-83 (protocol
versioning, raised by João, already decided and parked by him in-session),
ACT-84 (bun version mismatch, found during ACT-45's build).

Open set: 28 before this run's own count (doc-12 said 28), 31 now.

## Verified this run

- ACT-42's premise still holds. `target.ts` still raises the same
  undifferentiated message from two sites, unchanged in substance per
  doc-12's own re-check; this run did not re-verify line numbers again.
- ACT-73's, ACT-74's, and ACT-76's cited code still matches each card
  exactly: `assertComparableLineages`/`lineageDifferences` in attempts.ts
  are unmoved, and the only commits since these cards were filed
  (`ab05bc2`, `91495ba`) touch the rules tree, not corpus-file recording or
  comparison refusal.
- ACT-71's premise verified directly: `writeStageJudgeFailure`
  (run-abort.ts:92) still writes only `status`, `stage`, `error`, `input`,
  and the scorecard spread ACT-70 added; `pending.input` does not carry
  model/judgeModel/effort/judgeEffort, so a failed stage's artifact still
  can't answer "what model produced this." Distinct from ACT-70 (which
  fixed the judge's findings, not the run's own settings): not a duplicate.
- ACT-84's premise reproduces live, right now, in this shell: `bun
  --version` reports 1.4.1 against the repo's pinned 1.4.0 (`mise.toml`).

## Edits this run

| Card | Field | Before | After | Evidence |
| --- | --- | --- | --- | --- |
| ACT-73 | milestone | (unset) | m-1 | Found running ACT-39's own replay, same trust chain as ACT-63/64/68 |
| ACT-74 | milestone | (unset) | m-1 | Same replay, same chain |
| ACT-76 | milestone | (unset) | m-1 | Same replay; blocks exactly the comparison m-1 exists to produce |

No status, priority, assignee, or dependency field was changed beyond the
three milestone sets above. Nothing sits in Build or Review, so no card was
held by another session.

## Classification

No overtaken cards, no duplicates, nothing to archive this run.

- **ACT-71 is not a duplicate of ACT-70.** Same fix seam
  (`writeStageJudgeFailure`), different field: ACT-70 closed the judge's
  findings gap, ACT-71 is the run's own model/effort settings, still
  missing. Both cite the same function; that is a shared seam, not
  overlapping scope.
- **ACT-69 stays unassigned, deliberately, per its own notes**: it names its
  own blocker (a second pipeline case does not exist, and the report schema
  requires two) and its own cost range (0.43-0.53 USD per single failed
  shape attempt, times 2 cases x 3 arms x 2+ reps). Its notes already say
  "do not start without a number from João." This run does not re-propose
  starting it; it only confirms the blocker still holds (no second pipeline
  case exists in `cases/` today).
- **ACT-83 needs no action.** João already decided it in-session (deferred
  until beta) and the card records his own words and the reopening
  trigger. Correctly Low, correctly off the ready queue by its own text.
- **ACT-40 and ACT-44's relationship is unchanged from doc-12**: same
  defect class, deliberately split across milestones (ACT-44 was m-1 and
  is now Done; ACT-40 stays parked on m-5). Nothing to do.

## Doc-12's three questions, resolved by events or still open

1. **ACT-28 comment #5 (local-install corpus-freezing mechanism, a second
   provider coming): still unanswered.** ACT-37, ACT-63, ACT-68 are still
   built on it. ACT-63 closed anyway (per its own scope, latent, not
   blocking); ACT-37 and ACT-68 are still open and still waiting. Carried
   forward, unchanged, to the list below.
2. **ACT-63/64/68 priority: moot for 63 and 64 (both Done).** ACT-68 still
   carries no priority. Carried forward.
3. **ACT-59/60/61 milestone: still unset, still unanswered.** Carried
   forward, alongside a new instance of the same kind of call: ACT-71 also
   carries no milestone and plainly belongs to "tell improvement from
   noise" (m-3), since it blocks `comparison-comparability.ts`'s own
   comparability check.

## The queue

`backlog task list --ready --sort priority`, ordered against the live goal.

1. **ACT-42** (m-1, medium, independent). The last open card on m-1's own
   milestone; ACT-73/74/76 (also m-1 now) are independent of it and of each
   other.
2. **ACT-73, ACT-74, ACT-76** (m-1, no priority set yet, see question 2
   below). All three block trusting the comparison m-1 was built to
   produce: a false staleness positive, an unrecorded corpus input, and a
   refusal on the one comparison worth making.
3. **ACT-68** (m-1, no priority, waits on question 1).
4. **ACT-46** (m-2, high). Unchanged from doc-12: blocks a second person or
   João himself from running the tool with no flags.
5. **ACT-48 → ACT-52 → ACT-53, with ACT-49 alongside** (m-5, unchanged
   order from doc-7).
6. **ACT-37** (unassigned, waits on question 1).
7. **ACT-71, ACT-59 → ACT-60/ACT-61** (all unassigned; recommend m-3, see
   question 3 below).
8. **ACT-32 → ACT-35** (m-3), **ACT-30 and ACT-34** (m-2, parallel),
   **ACT-29** (m-2, alone, touches 44 test files), **ACT-84** (no
   milestone, cheap, real, found this week), **ACT-69** (blocked on
   João's budget number), **ACT-83** (parked, do not pick up),
   **ACT-26.7 → ACT-27 and ACT-31** (low, last).

### Bundles that cannot run in parallel

Unchanged from doc-12: ACT-26.7 before ACT-27/ACT-31 (same files); ACT-32
before ACT-35 (dependency); ACT-29 alone (44 test files). ACT-30 and ACT-34
still run in parallel. New: **ACT-73, ACT-74, ACT-76 touch different
functions** (stale's hash comparison, the build stage's corpus recording,
attempts.ts's lineage comparison) and can all run in parallel with each
other, with ACT-42 (target.ts), and with ACT-68 (checkpoint.ts).

## Kaizen candidate

None new. doc-12's candidate (a card's reproduction command should carry
the conditions the failure needs) is still a corpus edit and still out of
scope for a board triage.

## Questions to João

1. **ACT-28 comment #5 is still open and now blocks three cards
   (ACT-37, ACT-68, plus anything that assumes the local-install mechanism
   survives a second provider).** Recommend answering before any of the
   three starts, same reasoning as doc-12: a different mechanism changes
   what each should build, not just when.
2. **ACT-73, ACT-74, ACT-76, ACT-68, ACT-84 carry no priority.** Proposed:
   ACT-73 High (a false staleness positive teaches the operator to ignore
   a true one, on the tool's core promise), ACT-74 Medium (a real
   invalidation gap, narrower blast radius than 73), ACT-76 High (blocks
   the exact comparison m-1 exists to produce), ACT-68 Low (real, not
   urgent, per doc-12's own reasoning on the same chain), ACT-84 Low
   (cheap, real, but a chore, not blocking anyone who runs `mise` first).
   Recommend, priority is yours.
3. **ACT-59/60/61 and now ACT-71 carry no milestone.** Recommend m-3 for
   all four: they make evidence (context manifests, comparability)
   trustworthy the same way ACT-32 does, which is exactly what m-3 names.
   Carried forward from doc-12 with one addition (ACT-71); your call
   either way, and whichever way you answer applies to all four together
   since they're the same kind of gap.
