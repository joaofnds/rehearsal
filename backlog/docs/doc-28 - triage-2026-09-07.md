---
id: doc-28
title: triage-2026-09-07
type: other
created_date: '2026-09-07 10:45'
---

# Triage, 2026-09-07

Seventh triage on this board. Previous run: doc-23 (2026-09-06 12:09). Goal
(doc-6/doc-7, unchanged): prove the loop once (m-1), then the UI three (m-5,
m-6, m-7), then m-2, then m-3.

## What changed since doc-23

Four cards closed to Done on real fixes, each independently verified this run
by direct code inspection and a fresh full-suite run (1063/1063, typecheck and
lint clean): ACT-89 (session checks no longer count a seeded transcript
prefix), ACT-76 (corpus-only replay differences now print inline instead of
being refused), ACT-74 (a failed stage now carries the corpus files it
captured), ACT-63 (stale and replay now search the target's own corpus root,
not the control repository's). ACT-73 also closed, on observed behavior
(byte-identical and edited corpus files are correctly distinguished today);
the original false positive could not be reproduced because its evidence run
was git-ignored and is gone.

Four new cards were filed from that work: ACT-90 (archiving a card frees its
id for reuse, so two unrelated cards have already shared ACT-89's number),
ACT-91 (a stopped run with usable checkpoints is labelled not replayable,
confirmed by actually replaying one), ACT-92 and ACT-93 (two coverage gaps
left behind by ACT-63's fix: executeReplay's production wiring of corpusRoots
and stale's live-corpus branch are each provably correct today but nothing
would catch a regression).

Open set: 18 before (per doc-23's queue), 18 now (4 closed, 4 filed).

## Verified this run

Full suite fresh: 1063 pass, 0 fail, typecheck and lint clean. Working tree
clean at start and end.

ACT-87's premise re-confirmed directly: list-command.ts:153 still returns the
literal `"not replayable"` after successfully reading the stopped run's
manifest on the line above. ACT-91 is the same defect class at a different
call site (line 142's branch, the non-stopped path, is correct; line 153 is
not) — the two cards are siblings, not a duplicate, since they name different
branches with independent evidence.

ACT-84's premise re-confirmed: `REQUIRED_BUN_VERSION` (src/benchmark/config.ts)
is only checked inside `rehearsal.ts`'s own entry point; nothing gates bare
`bun test`, `bun run lint`, `bun run typecheck`, or `bun run fmt:check`, so the
card's fix is still unbuilt and the risk it names is still live.

**ACT-86's own record is false.** It claims 'landed 2026-09-06 as dotfiles
commit e33c7abf', naming a fix that puts `--append-notes` in the shape and
build skills at the point each writes the record. Checked directly: that
commit does not exist in `~/code/dotfiles` history, and neither
`skills/shape/SKILL.md` nor `skills/build/SKILL.md` names `--append-notes`
anywhere. The fix this card says it made was never made. Noted on the card;
not re-opened, since the card is still open and its own criterion #1 already
says it isn't checkable yet — this corrects the false intermediate claim so
the next session doesn't build on it.

**ACT-37's blocking premise is stale and the card is unblocked.** Three
consecutive triage docs (doc-16, doc-22, doc-23) carried forward "waits on
ACT-28 comment #5" as an open direction question. ACT-28's own notes, added
2026-09-06, state directly that the question is not open: João answered it on
DOT-36, and decision-4 (accepted, 2026-09-05) is that answer (declared and
live corpus both first-class), with ACT-37's harness-owned settings file named
in decision-4 itself as part of what the decision fixes. Restored to High
(doc-6 demoted it to Medium specifically for this reason) and moved into the
ready queue.

**ACT-49 cited a gap ACT-43 already closed.** ACT-49's description says the
design shows a per-run cost including the judge and that "ACT-43 records that
the artifact carries only the workflow session cost and omits the judge
entirely." ACT-43 is Done, verified against a real $12.43 run artifact whose
total equals every session role summed. Noted on ACT-49; the rest of its
inventory is untouched.

## Classification

No overtaken, duplicate, or out-of-scope cards found this run. ACT-90 through
ACT-93 are each independent, real, and correctly filed. ACT-83 is correctly
parked (deferred-until-beta, per its own recorded decision) and was skipped
from re-verification per that decision's own terms.

## Milestones

ACT-59, ACT-60, ACT-71 assigned to m-3. Doc-16, doc-22, and doc-23 each
recommended this, unopposed for three cycles; it groups existing To Do cards
into an existing milestone by the board's own established reasoning, not a
new priority judgment.

## The queue

`backlog task list --ready --sort priority`, ordered against the live goal.
ACT-88 appears in that command's output despite sitting in Shape with an open
question in its own text; excluded here per the board rule that a card asking
a question is not ready.

1. **ACT-68** (m-1, no priority, independent, no longer waiting on anything).
2. **ACT-37** (now High, unblocked this run). Independent of ACT-68.
3. **ACT-48 → ACT-52 → ACT-53, with ACT-49 alongside** (m-5, High, unchanged).
4. **ACT-59** (m-3, High, now milestoned) **→ ACT-60** (m-3, Medium).
5. **ACT-71** (m-3, no priority, independent of ACT-59/60).
6. **ACT-32** (m-3, Medium) **→ ACT-35** (m-3, Medium, waits on ACT-32 and
   money, ~7 USD, already approved per ACT-35's own text).
7. **ACT-84** (Medium, cheap, real, independent).
8. **ACT-69** (blocked on João's budget number, largest spend on the board).
9. **ACT-87, ACT-90, ACT-91, ACT-92, ACT-93** (no priority; small, independent
   defects/coverage gaps, none milestone-urgent). ACT-92 and ACT-93 can run in
   the same session per doc-27's own recommendation (one seam may serve both).
10. **ACT-86** (partial, prose fix not actually landed per this run's finding;
    needs a session to actually write the `--append-notes` line before
    criterion #1 is checkable).
11. **ACT-83** (parked, do not pick up before a beta milestone exists).
12. **ACT-26.7 → ACT-27, ACT-31** (Low, last, same files).

Bundles that cannot run in parallel: ACT-26.7 before ACT-27/ACT-31 (same
files); ACT-32 before ACT-35 (dependency); ACT-59 before ACT-60 (dependency).
Everything else in the queue touches independent files.

## Kaizen candidates

**Recurring, from doc-26 and doc-27, now with a third instance.** Three
successive m-1 reliability cards (ACT-73, ACT-74, ACT-76) each closed on
inference over shared code rather than a live pipeline run reaching a stage
judge on both a pass and a grade failure. Doc-26 proposed the live run that
would upgrade all three from inference to observed fact; nothing has run it
yet. Separately, doc-27 flagged a sentinel-argument smell (an `undefined`
branch standing in for a different caller's intent) that cost ACT-63 two
extra review-driven commits, and suggested a named check for it in
review-code's architecture axis if it recurs. Recommending both to kaizen by
name; not started here.

**New this run.** ACT-86 is the second card this board has closed (or nearly
closed) on a claimed fix that turns out, on direct inspection, not to exist in
the target repository (the commit it cites is not in history). The first
instance of this pattern was this run's own ACT-86 finding; there's no second
instance yet to call it recurring, but it's worth the next triage watching for
a repeat, since an unverified "landed" claim inside a card's own notes is
exactly the kind of thing triage's premise-check exists to catch and this one
sat since 2026-09-06 before being caught.

## Questions to João

1. **ACT-88's scope** (carried unanswered from doc-23): should preflight
   validation cover only `run`, or also `replay` and `calibrate`? Should model
   availability be checked with a real call, or skipped/stubbed for the test
   suite? Recommend: gate all three commands (validation is about
   references being resolvable, which none of the three should proceed
   without), and check model availability by name against a static known-model
   list rather than a network call, so the suite stays fast and offline.
2. **ACT-61's open question** (carried unanswered from doc-23): does a case
   declare its project context explicitly, or does the harness discover it
   from the target tree? Recommend explicit declaration, for the same reason
   ACT-88 recommends checking references up front: a discovered document can't
   be told apart from one that merely happened to be present, and this
   project's whole premise rests on evidence being attributable.
3. **Priorities for the five newest small defects** (ACT-87, ACT-90, ACT-91,
   ACT-92, ACT-93), all currently unset. Recommend: ACT-91 Medium (a stopped
   run is the case an operator most wants to replay, and the label actively
   lies about it), ACT-87 Medium (same ENOENT class already fixed twice
   elsewhere), ACT-90 Low (real but nothing is broken by it today), ACT-92 and
   ACT-93 Low (coverage gaps on code already confirmed correct this run and
   last).
