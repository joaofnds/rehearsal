---
id: doc-33
title: triage-2026-09-07-b
type: other
created_date: '2026-09-07 16:25'
---

# Triage, 2026-09-07 (second pass, 16:20)

Eighth triage on this board. Previous run: doc-28 (2026-09-07 10:45), 5.5
hours earlier. Goal (doc-6/doc-7, unchanged): prove the loop once (m-1), then
the UI three (m-5, m-6, m-7), then m-2, then m-3.

## What changed since doc-28

Five cards closed to Done on real fixes since the last run, each backed by a
commit: ACT-37 (harness-owned settings file, no hooks), ACT-48 (design
vocabulary settled in GLOSSARY.md, decision-5), ACT-52 (design system built,
suite green), ACT-87 (list runs no longer leaks raw ENOENT for a
manifest-less stopped run), ACT-91 (a stopped run with usable checkpoints is
now listed replayable). ACT-94 also closed (client test preload fixed, full
suite green). Four reflection docs were filed for these (doc-29 through
doc-32), each read this run.

This run closed one more: **ACT-32** (transcript checks now score the
records after the case's declared cut, not the resumed prefix). Its own
`#defect` label and To-Do status were stale — commit 2229463 (2026-09-06,
authored directly by João, not a session) already fixed it, with a test.
Verified directly this run: `session-attempt.ts:334-335` slices
`toolUses(transcript.slice(cut))`, `files-read` consumes the same sliced
array, and a fresh full suite passes (1088+62, 0 fail, typecheck/lint clean).
Closed citing the commit. This unblocks **ACT-35**, which was waiting on it
and already carries João's approval for its ~7 USD spend.

Open set: 18 before (per doc-28's queue), 18 now (1 closed this run: ACT-32;
1 filed: ACT-95; 5 closed since doc-28 by other sessions: ACT-37, ACT-48,
ACT-52, ACT-87, ACT-91, offset by cards those sessions filed).

## Verified this run

Full suite fresh: 1088 pass (main) + 62 pass (client), 0 fail, typecheck and
lint clean. Working tree clean at start; board-only changes at end (task
files, one new task, this doc).

A dedicated reviewer sub-agent checked the checkable premises of 17 open
cards against current code (ACT-50, ACT-51, ACT-53, ACT-49, ACT-59, ACT-60,
ACT-71, ACT-68, ACT-84, ACT-32, ACT-69, ACT-90, ACT-92, ACT-93, ACT-27,
ACT-31, ACT-26.7). Its ACT-32 finding was independently confirmed above
(code read directly, suite run fresh) before acting on it, per this board's
own rule that a sub-agent's report is not itself evidence.

**ACT-27's blocking premise is stale; the card itself is not.** The card
blames `import/prefer-default-export` for reverting a prior attempt to split
`run-command.ts`/`replay-command.ts`'s harness wiring from command policy.
That lint rule is now off (`.oxlintrc.json:90`, commit bf90120, 2026-09-07),
so the obstacle is gone. The underlying cost claim is unchanged and worse:
`run-command.ts` is 424 lines and `replay-command.ts` 403, both grown since
the card was written (236/282). Noted on the card; still Low, still queued
behind ACT-26.7 (same files as ACT-31).

**ACT-49, ACT-59, ACT-60, ACT-68, ACT-69, ACT-71, ACT-84, ACT-90, ACT-92,
ACT-93** all confirmed HOLDS against current code, no stale citations beyond
what doc-28 already corrected on ACT-49. Detail is in the sub-agent's report,
not repeated here; nothing in it changed a status or priority beyond what
follows.

**ACT-50, ACT-51, ACT-53** confirmed still genuinely unbuilt: `client/src/`
holds only the design-system layer (ACT-52's scope), no server, no routes,
no data wiring. No change.

## Classification

No overtaken or duplicate cards found beyond ACT-32. ACT-92/ACT-93 remain
sequenced, not duplicate, per their own cross-reference (unchanged from
doc-28). ACT-83 remains correctly parked and was skipped, per its own
recorded decision.

## New card

**ACT-95** (Medium, no milestone): `rehearsal stale` does not hash a pipeline
checkpoint's settings file, so a settings-only edit reports fresh. Filed from
ACT-37's own reflection (doc-29), which named this gap outside its four
acceptance criteria. Independent of ACT-37 (Done) and ACT-59/60 (m-3,
different corpus kind, already in flight).

## Priorities set

ACT-90, ACT-92, ACT-93 set to Low, per doc-28's carried recommendation
(unopposed for one cycle, restated with reasons there): ACT-90 is real but
nothing is broken by it today; ACT-92 and ACT-93 are coverage gaps on code
already confirmed correct two runs running. ACT-87 and ACT-91, also on
doc-28's priority list, are Done and need no priority.

## The queue

`backlog task list --ready --sort priority`, ordered against the live goal.

1. **ACT-35** (m-3, Medium). Newly unblocked this run (ACT-32 closed);
   ~7 USD, already approved per its own text.
2. **ACT-49** (m-5, High, independent) **alongside ACT-53** (m-5, High,
   waits on ACT-52, now Done — ready).
3. **ACT-59** (m-3, High) **→ ACT-60** (m-3, Medium).
4. **ACT-71** (m-3, no priority, independent of ACT-59/60).
5. **ACT-84** (Medium, cheap, real, independent).
6. **ACT-69** (blocked on João's budget number, largest spend on the board).
7. **ACT-90, ACT-92, ACT-93** (Low, small, independent; ACT-92/ACT-93 can run
   together per doc-27's recommendation, one seam may serve both).
8. **ACT-95** (Medium, independent, newly filed).
9. **ACT-86** (still blocked on itself: the `--append-notes` prose fix its
   own notes claimed was landed was never made; needs a session to actually
   write it before criterion #1 is checkable).
10. **ACT-83** (parked, do not pick up before a beta milestone exists).
11. **ACT-27** (Low, unblocked lint-wise, still queued after ACT-26.7 for
    file overlap) **→ ACT-31** (Low, same files as ACT-27).
12. **ACT-26.7** (Low, ahead of ACT-27/ACT-31 per file overlap).

Bundles that cannot run in parallel: ACT-26.7 before ACT-27/ACT-31 (same
files); ACT-59 before ACT-60 (dependency). Everything else touches
independent files. ACT-68, ACT-51, ACT-50, ACT-92/93 remain independent
small items not yet in the front of the queue but pickable any time.

ACT-61 and ACT-88 remain in Shape with open questions in their own text
(doc-28's questions 1 and 2, still unanswered) and are excluded from ready,
per the board's own rule.

## Kaizen candidates

**Recurring, now three named instances.** Doc-28 named ACT-86 as the first
instance of a card's own notes claiming a fix landed that direct inspection
shows was never made. Doc-32 (ACT-48's reflection) names a second: the shape
session that first drafted ACT-48 recorded decision-5 as accepted and closed
the card before João's actual acceptance was on record. This run adds no
third instance but the pattern is now two independent occurrences across two
different sessions doing two different kinds of work (a chore card's
completion claim, a shape session's decision-recording), which is stronger
grounds than doc-28 had to raise it to kaizen now rather than keep watching.
Recommending to kaizen by name.

**Recurring, unchanged from doc-26/doc-27/doc-28.** Three successive m-1
reliability cards closed on inference over shared code rather than a live
pipeline run reaching a stage judge on both a pass and a grade failure.
Nothing has run the live check yet. Still recommending to kaizen; not started
here.

**New this run.** Doc-30 (ACT-91's reflection) flags that the iteration
guard fires on a session's own `@claude` assignee bookkeeping (the board's
own pickup rule) rather than on another session actually holding the card
live, which stopped an automated iteration from continuing. Worth kaizen
checking whether the guard should distinguish "assignee set by the session
now running" from "assignee set by a different, possibly dead, session."
Recommending to kaizen by name; not started here.

## Questions to João

Doc-28's first two questions are still open; nothing this run answers them.
Its third (priorities for five small defects) is settled above. Restating
the remaining two rather than re-deriving:

1. **ACT-88's scope**: preflight for `run` only, or also `replay` and
   `calibrate`? Model availability checked live or against a static list?
   Doc-28's recommendation stands: gate all three, check by name against a
   static list.
2. **ACT-61's open question**: does a case declare its project context
   explicitly, or does the harness discover it? Doc-28's recommendation
   stands: explicit declaration, for attributability.
