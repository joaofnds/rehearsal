---
id: doc-41
title: triage-2026-09-08-c
type: other
created_date: '2026-09-08 12:27'
---

# Triage, 2026-09-08 (c)

Twelfth triage on this board. Previous run: doc-40 (2026-09-08 12:19), about
two hours earlier. Goal (doc-6/doc-7, unchanged): prove the loop once (m-1),
then the UI three (m-5, m-6, m-7), then m-2, then m-3.

## What changed since doc-40

ACT-35 closed to Done (all four acceptance criteria checked): the three
unrun brief-reply turns ran, one debug rep each at --model sonnet, 3.30 USD
against a ~7 USD projection. Word bands recorded for all four cases; only
one lands inside its own accepted length. That finding was filed as a new
card, **ACT-119** (word-band's shared max of 154 passes replies up to twice
a turn's real accepted length), referenced from ACT-35's own notes so the
finding isn't left only in Done-card prose. Open set: 32 before, 32 now net
(ACT-35 closed, ACT-119 opened).

## Verified this run

Read doc-40 in full and every commit since its timestamp (5 commits: the
ACT-35 stale-probe note, the ACT-35 close plus ACT-119 filing). Read ACT-35,
ACT-119, ACT-118, ACT-116, ACT-117, ACT-115, ACT-61, and ACT-86 directly.
Confirmed no card is in Build or Review and no session holds anything. No
reflection doc newer than doc-39 (ACT-35 closed without a separate
reflection doc; its own implementation notes carry the equivalent detail and
already route the one finding to ACT-119).

Checked doc-40's two open questions against current card state:

- **ACT-86**: doc-40 asked whether to re-land `--append-notes` as its own
  isolated commit. That premise is now moot: ACT-86's own notes (dated
  2026-09-08, after doc-40) record that the isolated-commit diagnosis was
  itself wrong (the fix was never reverted; it was written on a session
  checkpoint ref in `~/code/dotfiles` and never reached main). ACT-115 was
  filed to re-land it as an ordinary edit on main. ACT-86 correctly stays
  open, blocked on ACT-115 landing and a later session's card write being
  observed using the flag. No further action; this question is resolved by
  work already done.
- **ACT-61**: unchanged. Still Shape, still 0/4 acceptance criteria, still
  carrying the 2026-09-07 direction note that answers its own open question.
  Sixth run in a row read and left untouched. Carried below again.

## Classification

No overtaken or duplicate card. ACT-119 is its own instance, not a
restatement of ACT-104 (which is about rendering an interval, not about what
the band's bound should be).

## Priorities

Set **ACT-119 to Medium** (was unset): same shape and size as ACT-104 and
ACT-113 at filing, a small, independently buildable check-parameter fix with
no blockers, filed from a session that already did the measurement it needs.

## The queue

`backlog task list --ready --sort priority`. Unchanged from doc-40 except
ACT-35 dropping out (closed) and ACT-119 joining the Medium tier.

1. **ACT-114** (m-7, Medium): still the closer of m-7's own named risk (never
   proven against a real recorded comparison).
2. **ACT-60** (m-3, Medium): doc-39's named next step, unblocked since
   doc-40.
3. **ACT-71, ACT-84** (independent, Medium/unset).
4. **ACT-69** (blocked on João's budget number).
5. **ACT-90, ACT-92, ACT-93** (Low, independent; ACT-92/93 can run
   together).
6. **ACT-95, ACT-113, ACT-116, ACT-117, ACT-118, ACT-119** (Medium,
   independent of each other and the rest). ACT-118 still worth picking up
   before or alongside ACT-60/ACT-61 if either extends the manifest-probe
   fixture, per doc-39.
7. **ACT-115** (Medium): a one-session prose re-land, unblocks ACT-86's own
   pending mechanism.
8. **ACT-106** (independent, unblocked, right-sized; no milestone).
9. **ACT-96-105, ACT-109, ACT-112** (independent backend gaps).
10. **ACT-86** (blocked on ACT-115 landing, then on observing a future
    session's card write).
11. **ACT-83** (parked).
12. **ACT-27** (Low) → **ACT-31** (Low), still behind **ACT-26.7** (Low).

Bundles that cannot run in parallel: unchanged from doc-40 (ACT-60/ACT-118
if ACT-60 touches the manifest-probe fixture; ACT-26.7 before ACT-27/ACT-31;
ACT-114's check and ACT-109's schema change touch the same comparison-record
path).

## Kaizen candidates

None new. The two already carried since doc-33/doc-36/doc-38 are unchanged
and still not started.

## Questions to João

1. **ACT-61**: unblocked and answered since 2026-09-07, still sitting in
   Shape with nothing built, now a sixth run flagging it. Recommending: move
   to To Do (priority already Medium).
