---
id: doc-22
title: triage-2026-09-06
type: other
created_date: '2026-09-06 01:19'
---

# Triage, 2026-09-06

Fifth triage on this board. Previous run: doc-16 (2026-09-05 21:22). Newest
reflections read first: doc-19 (ACT-85), doc-20 (ACT-30), doc-21 (ACT-34).
None changes the plan; all three confirm their own card's bet held and
propose no new card.

The goal is still doc-6/doc-7's: prove the loop once (m-1), then the UI
three (m-5, m-6, m-7), then m-2, then m-3.

## What changed since doc-16

Closed since doc-16: ACT-30, ACT-34 (both confirmed Done by their own
reflections, doc-20/doc-21), and ACT-42 (closed this run, see below).

A commit landed mid-run, not authored by this triage: `c6e2dba` merged
ACT-89 into ACT-84 as duplicates and archived ACT-89. This run had
independently reached the same conclusion (see below) before finding the
merge already done. No action needed; noted as confirmation.

Open set: 31 before this run (doc-16's count), 28 now.

## Closed this run

**ACT-42 closed as overtaken by ACT-72 (Done).** ACT-42 asked that a
baseline-validation failure name which condition fired (wrong branch, wrong
SHA, dirty worktree) instead of one shared string. ACT-72, closed
2026-09-05 on commit `ed10c54`, already rewrote both raise sites in
`target.ts` to do exactly that: `describeBranchDrift` names both branches,
the commit-mismatch message names both SHAs, `describeDirtyWorktree` lists
the offending paths. Verified directly against `target.ts:208-299` this
run: all four of ACT-42's own acceptance criteria are met by the code as it
stands. ACT-42 was last updated 2026-09-04 23:01, before ACT-72 existed,
and was never re-checked against it. All four criteria checked with that
evidence; the card was carrying doc-16's own re-verification of the
symptom, not of whether it was already fixed elsewhere.

## Verified this run

- ACT-63's premise verified true, and **doc-16's own claim about it is
  wrong**: doc-16 line 25 lists ACT-63 as Done. It is not; `backlog task
  view ACT-63` shows status To Do, unchanged since 2026-09-04 23:01. The
  underlying defect (`stageCorpusRoots` resolving to `CONTROL_DIR` instead
  of the target root) is real, latent (no `.claude` in either repo root
  today), and still on m-1 with no priority set. This is a correction to
  doc-16's record, not a card action; doc-16 stands as written elsewhere.
- ACT-68's premise verified true, unchanged: `checkpoint.ts:194-196` and
  `:233-235` still take `instructions` as a bare string.
- ACT-71's premise verified true directly against current code:
  `StageJudgeInput` (`contracts.ts:251-268`) carries no model, judgeModel,
  effort, or judgeEffort field, and `writeStageJudgeFailure`
  (`run-abort.ts:92-114`) writes `pending.input` whole, so a failed stage's
  artifact still can't answer what model or effort produced it.
- ACT-73, ACT-74, ACT-76's cited code and behavior unchanged since doc-16;
  re-confirmed against current `attempts.ts` and `target.ts` (ACT-76's
  refusal site, `assertComparableLineages`, is unmoved at `attempts.ts:271`).
- ACT-29's counts drift is immaterial: `grep -rln mkdtemp src/ | wc -l` = 45
  today against the card's last recorded 44; same class of leak, same
  order of magnitude.
- ACT-32's premise verified true: `session-attempt.ts:329` still evaluates
  `toolUses(transcript)` over the whole transcript, no prefix cut.
- ACT-87's premise verified true: `list-command.ts` still calls
  `loadRunManifest` with no existence check before parsing, same shape as
  the defect ACT-40 and ACT-85 already fixed elsewhere.
- ACT-88's premise verified true in part: `assertControlReady`/
  `assertSourceReady` are called from both `run.ts` and `run-command.ts`,
  covering target readiness only, not the other declared references
  (task/brief/rubric/pipeline files, model, budget) the card's acceptance
  criteria also name.
- A full run of `mise exec -- bun test` passes clean (1052 pass, 0 fail);
  the bare `bun test` fails 48, all attributable to the bun-pin mismatch
  ACT-84 now owns (see below), confirming the mismatch is the whole cause
  and not masking anything else.

## ACT-84 / ACT-89: confirmed duplicate, already resolved

This run independently identified ACT-84 and ACT-89 as the same defect
(pinned bun 1.4.0 vs ambient 1.4.1, unrelated tests fail with no version
signal, cost two separate sessions three hours apart) before discovering
`c6e2dba` had already merged them: ACT-89 archived, its evidence and its
third acceptance criterion (remove the CLAUDE.md instruction once a
mechanism makes it unnecessary) carried onto ACT-84. No further action; the
merge is correct and matches what this run would have proposed.

## Classification

No new overtaken cards beyond ACT-42, no duplicates beyond the
already-resolved ACT-84/ACT-89, nothing else to archive this run.

- **ACT-74 is unverifiable by static code read, and that's expected, not a
  gap.** Its claim is about a specific run artifact's recorded fields, not
  a code-path asymmetry that a grep settles. It stands as written.
- **ACT-63 and ACT-59/60/61 are adjacent, not overlapping.** ACT-63 is
  which root directory a stage's corpus search starts from; ACT-59/60/61
  are which corpus kinds are observed and recorded. Different layers, no
  merge.

## Doc-16's open questions, carried forward unchanged

Nothing since doc-16 answered any of these. Restated below rather than
re-argued.

## The queue

`backlog task list --ready --sort priority`, ordered against the live goal.

1. **ACT-73, ACT-74, ACT-76** (m-1, no priority yet). All three block
   trusting the comparison m-1 exists to produce. Independent of each
   other and of everything else on m-1.
2. **ACT-63** (m-1, no priority yet, corrected above from doc-16's wrong
   Done claim). Latent, real, independent of ACT-73/74/76.
3. **ACT-68** (m-1, no priority, waits on ACT-28 comment #5).
4. **ACT-46 chain is Done**; next on m-2 per doc-20/doc-21 is **ACT-88**,
   ahead of ACT-29 (load-bearing for "a second person can run the tool" in
   a way ACT-29 is not).
5. **ACT-48 → ACT-52 → ACT-53, with ACT-49 alongside** (m-5, unchanged).
6. **ACT-37** (unassigned, waits on ACT-28 comment #5).
7. **ACT-71, ACT-59 → ACT-60/ACT-61** (unassigned; doc-16 recommended m-3
   for all four, unanswered, carried forward).
8. **ACT-32 → ACT-35** (m-3), **ACT-29** (m-2), **ACT-84** (no milestone,
   cheap, real, now carries ACT-89's third criterion too), **ACT-69**
   (blocked on João's budget number), **ACT-83** (parked), **ACT-87**
   (m-5's own gap, per doc-19 recommend Medium, ahead of the unprioritized
   m-3 cluster, behind m-5's screen cards), **ACT-86** (chore, 1/2 ac),
   **ACT-26.7 → ACT-27 and ACT-31** (low, last).

### Bundles that cannot run in parallel

Unchanged from doc-16: ACT-26.7 before ACT-27/ACT-31 (same files); ACT-32
before ACT-35 (dependency); ACT-29 alone (44+ test files). ACT-73, ACT-74,
ACT-76, ACT-63, ACT-68 all touch different functions and can run in
parallel with each other and with ACT-88 (run.ts/run-command.ts
preflight).

## Kaizen candidate

**Doc-16 carries a factual error** (ACT-63 marked Done when it was not),
caught only because this run happened to verify that card's status
directly rather than trusting the previous triage doc's summary line. One
line for kaizen: a triage doc's own "closed since last run" list is a claim
like any other and should be spot-checked against `backlog task view`, not
copied from memory of what a session intended to close.

**ACT-89 was filed without searching the board first**, duplicating ACT-84
one day later. Already named as a kaizen candidate implicitly by João's own
merge commit message; not re-raised here as a new instance, since it is the
same finding.

## Questions to João

1. **ACT-73, ACT-74, ACT-76, ACT-63, ACT-68, ACT-84 carry no priority
   between them except ACT-84 (medium).** Carried forward from doc-16 with
   ACT-63 added (corrected onto the open list this run): recommend ACT-73
   High, ACT-76 High, ACT-74 Medium, ACT-63 Medium (latent but on the same
   m-1 trust chain), ACT-68 Low. Priority is yours.
2. **ACT-28 comment #5 (does local-install corpus-freezing survive a
   second provider) is still open** and still blocks ACT-37 and ACT-68.
   Carried forward unchanged from doc-16: recommend answering before either
   starts.
3. **ACT-59, ACT-60, ACT-61, ACT-71 still carry no milestone.** Carried
   forward unchanged from doc-16: recommend m-3 for all four.
