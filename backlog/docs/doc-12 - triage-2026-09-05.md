---
id: doc-12
title: triage-2026-09-05
type: other
created_date: '2026-09-04 23:01'
---

# Triage, 2026-09-05

Third triage on this board. The previous run is doc-5 (2026-09-04). The newest
reflection is doc-11 (ACT-65), read first as the skill directs; it changes
nothing about the plan, and its own follow-up (ACT-68) is already filed.

## What changed since the last run

The last triage (doc-5) left the goal empty and three questions open. None of
the three was answered by a session acting as triage; they were overtaken by a
planning session the same day (doc-7, 14:40) that met question 1 directly.

- **The goal is no longer empty.** doc-7 set the milestone order:
  m-1 (prove the loop once) first, then the UI three in order (m-5, m-6, m-7),
  then m-2 (a second person can run it), then m-3 (tell improvement from
  noise) last. This run prioritizes against that order.
- Closed since doc-5: ACT-41 (6/6), ACT-66 (12/12), ACT-67 (3/3), ACT-65
  (4/4). None of the doc-5 queue (ACT-37, ACT-32, ACT-30, ACT-34, ACT-29,
  ACT-35) was touched; all six are still exactly where doc-5 left them.
- Created since doc-5: ACT-40, 42, 43, 44, 45, 46 (m-1/m-2 defects found
  running ACT-38), ACT-47 through ACT-53 (UI planning, decisions 2 and 3),
  ACT-59, 60, 61 (context-manifest design, doc-9), ACT-63, 64 (found in
  ACT-41's own review and its verification run), ACT-68 (found in ACT-65's
  review).
- Open set: 9 before doc-5's own count, 28 now.
- doc-7 already flagged seven duplicate UI cards filed and caught in the same
  session; they are archived (act-52 through act-58 in the archive) and
  correctly excluded from the count above.

## Why this run did not shrink the open set by closing anything

Every open card was checked against the four closure citations. None is
addressed by a commit since doc-5 (the log is ACT-41, ACT-65/68, ACT-66/67,
and UI/decision docs; no commit touches code any other open card names). None
is overtaken by Done work. None is superseded by another open card. None is a
duplicate beyond the seven doc-7 already caught. None is out of scope. "No
consequence for the goal" cannot be argued for the m-1 cards, and the rest
either serve a later milestone doc-7 already ordered or are explicitly
unassigned pending a decision, which is not the same as no consequence.

Three cards were re-measured and hold with a dated note (ACT-29, ACT-42);
ACT-44's premise was re-verified live and is unchanged. See below.

## The premise gap this run found: three m-1 cards were invisible to the plan

doc-7 set m-1's order as ACT-41 → ACT-39, with ACT-42, ACT-43, ACT-44
independent, and was written at 14:40 on 2026-09-04. ACT-63 and ACT-64 were
filed at 17:30 and 17:38 that day, and ACT-68 at 22:48, all after doc-7 and
all on the same trust chain ACT-41 and ACT-65 were built to fix: whether a
stage session reads the corpus the run record says it read.

None of the three carried a milestone. Fixed this run, since the card text
plainly places them on m-1 and the field simply hadn't been set:

- **ACT-63** (bug, no priority): the same control-repository/target
  conflation ACT-41 fixed for CLAUDE.md, one layer down, for corpus roots.
  Verified live: `CONTROL_DIR` is still threaded into `corpusLayoutRoots` at
  three call sites (checkpoint.ts:122, run-command.ts:283,
  replay-command.ts:245). Verified latent, not active: neither
  `/Users/joaofnds/code/rehearsal/.claude` nor the audit-log target's
  `.claude` exists, so it does not block ACT-39 today, only the moment
  either repository gains a project-level corpus directory.
- **ACT-64** (bug, no priority): a corpus/agent-behavior finding, not a
  harness defect. Its single acceptance criterion is really an observation
  (does the shape stage complete clean on a re-run), not a code fix.
- **ACT-68** (bug, no priority, depends on ACT-65 which is Done): the
  design asymmetry that let ACT-65's bug exist. Real, not urgent, correctly
  scoped as a follow-up per doc-11.

All three now carry `milestone: m-1`. None had its priority set by this run;
see question 2.

## Every edit, with evidence

| Card | Field | Before | After | Evidence |
| --- | --- | --- | --- | --- |
| ACT-29 | notes | 2026-09-04 note | + 2026-09-05 note | `grep -rln 'mkdtemp' src/ \| wc -l` → 44 (was 45), `TestResources` → 24 unchanged, leak 1075 → 1072 |
| ACT-42 | notes | (none) | + 2026-09-05 note | Raise sites moved from target.ts:214/248 to :223/257, same two sites |
| ACT-44 | notes | (none) | + 2026-09-05 note | `./rehearsal.ts list runs` still prints raw ENOENT for both stopped runs on disk |
| ACT-63 | milestone | (unset) | m-1 | Card's own text places it on ACT-41's chain; doc-7 predates the card |
| ACT-64 | milestone | (unset) | m-1 | Found inside ACT-41's verification run, same chain |
| ACT-68 | milestone | (unset) | m-1 | Depends on ACT-65 (m-1's ACT-39 gate), doc-11 confirms the chain |

No status, assignee, or dependency field was changed. No card sits in Build or
Review, so nothing was held by another session.

## Stale premises found

### ACT-29's counts moved slightly; conclusion unchanged

`mkdtemp` sites: 45 (doc-5) → 44 now. `TestResources`: 24, unchanged. Visible
leak: 1075 → 1072. None of this changes the card's size or priority; recorded
per decision-1.

### ACT-42's line numbers moved; same defect

Both raise sites moved nine lines down between doc-5 and now (unrelated
commits touching target.ts above them). Same message, same two conditions
conflated. No consequence.

### ACT-44 still reproduces exactly as filed

`list runs` throws raw ENOENT for both stopped runs currently on disk. This is
independent of ACT-63/64, which touch a different failure class filed later
against the same milestone.

## Classification

No overtaken cards, no duplicates beyond the seven doc-7 already archived, no
cards to archive this run.

- **ACT-30 and ACT-34 remain confirmed non-duplicates** (doc-5's finding,
  unchanged; not re-measured this run since neither has moved).
- **ACT-40 and ACT-44 are the same defect class (raw filesystem error
  reaching the operator from a lister) at two call sites, not one card.**
  Both cite the same rule (`run-layout.ts`'s `entries` catches and returns
  `[]`) and ACT-40's own notes say fixing it in the shared read path
  (`listRecords`) fixes both surfaces at once. They are not proposed as a
  merge because ACT-40 is deliberately parked on m-5 to land with the UI's
  shared read path, while ACT-44 is m-1 and independent of the UI. Splitting
  them across milestones was doc-7's own deliberate choice, not an oversight,
  so this run leaves both as they are and only notes the relationship for the
  next reader.

## The queue

`backlog task list --ready --sort priority`, ordered against m-1 (the live
goal) first, then doc-7's stated order for what follows.

1. **ACT-41 → ACT-39** is already Done → ready; ACT-39 sits at the head,
   both dependencies Done, waiting on nobody.
2. **ACT-42, ACT-43, ACT-44** (m-1, independent of ACT-39 and of each other).
   Fresh reproductions all still hold.
3. **ACT-63** (m-1, latent, no priority set). Worth fixing before it goes
   live, cheap, but blocks nothing today.
4. **ACT-64** (m-1, no priority set). An observation, not a build; likely
   closes on a clean re-run of the shape stage rather than a code change.
5. **ACT-68** (m-1, depends on Done ACT-65, no priority set). Real, not
   urgent, per doc-11.
6. **ACT-45, ACT-46** (m-2). Both real, both block a second person or João
   himself from running the tool at all.
7. **ACT-48 → ACT-52 → ACT-53, with ACT-49 alongside** (m-5, the UI's first
   slice, per doc-7's own order).
8. **ACT-37** (unassigned, deliberately: waits on ACT-28 comment #5, see
   question 1 below).
9. **ACT-32 → ACT-35** (m-3), **ACT-30 and ACT-34 in parallel** (m-2),
   **ACT-29** (m-2, alone), **ACT-59 → ACT-60 and ACT-61** (unassigned to
   any milestone; see question 3), **ACT-26.7 → ACT-27 and ACT-31**
   (unassigned, low, per doc-7's own reasoning: last work, not next, unless
   ACT-26.7 is pulled forward by something scripting `run --json`).

### Bundles that cannot run in parallel

Unchanged from doc-5, carried forward: ACT-26.7 before ACT-27/ACT-31 (same
files); ACT-32 before ACT-35 (dependency); ACT-29 alone or first (touches 44
test files). ACT-30 and ACT-34 still run in parallel, per doc-5.

One addition: **ACT-63 and ACT-42** touch different functions in different
files (checkpoint.ts/run-command.ts/replay-command.ts vs target.ts) and can
run in parallel with each other and with ACT-44 (list-command.ts).

## Kaizen candidate

None new this run. doc-5's kaizen (a card's reproduction command must carry
the conditions the failure needs) is not yet carried into the shape/build
skills as a corpus change; that is a corpus edit, out of scope for a board
triage, and is noted here only so the next triage doesn't re-propose it.

## Questions to João

1. **ACT-28 comment #5 is still open, and it is bigger than ACT-37 waiting
   on it.** You wrote that the local-install corpus-freezing mechanism keeps
   costing you in per-machine setup complexity, and that a second provider
   (Gemini, etc.) will make it worse, not better. ACT-37, ACT-63, and ACT-68
   are all built on that mechanism continuing to exist. Recommend answering
   this before any of the three starts, since a different mechanism (not a
   local install at all) would change what each of them should build rather
   than merely delay them.
2. **ACT-63, ACT-64, ACT-68 carry no priority.** Proposed: ACT-63 Medium (real,
   cheap, currently latent), ACT-64 Low (an observation pending a clean
   re-run, not a build), ACT-68 Low (correctly deferred per doc-11, not
   urgent). Recommend, but priority between these and the rest of m-1 is
   yours.
3. **ACT-59, 60, 61 (the context-manifest chain) carry no milestone.** They
   are real, sequenced, and doc-9 designs them, but doc-7 never placed them
   in the m-1/m-2/m-3/UI order. Recommend m-3 (they make session-case
   evidence trustworthy the same way ACT-32 does, and m-3 is "tell
   improvement from noise"), but they could also sit with m-1 if you'd
   rather the pipeline-case trust chain (ACT-41/63/65/68) and the
   session-case trust chain (ACT-59/60/61) close together. Your call.
