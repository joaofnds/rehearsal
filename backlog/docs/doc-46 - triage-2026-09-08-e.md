---
id: doc-46
title: triage-2026-09-08-e
type: other
created_date: '2026-09-08 20:41'
---

# Triage, 2026-09-08 (e)

Fourteenth triage on this board. Previous run: doc-43 (2026-09-08 16:51), about
four hours earlier. Goal (doc-6/doc-7, unchanged): prove the loop once (m-1),
then the UI three (m-5, m-6, m-7), then m-2, then m-3.

## What changed since doc-43

Two reflection docs landed since doc-43 and outrank it: doc-44 (reflection on
ACT-60, 2026-09-08 17:13) and doc-45 (reflection on ACT-61, 2026-09-08 20:16).
Both cards they reflect on are Done. Doc-44 proposed no reorder (ACT-71 already
correctly queued as m-3's last item). Doc-45 proposed moving ACT-60 to the
front of the m-3 queue; that proposal is moot, since ACT-60 closed to Done the
same day, before this run started. Nothing further to act on from either doc.

Commits since doc-43 (dfd96bf) are all ACT-60's build and review, ACT-61's
shape/build/review, one priority change (ACT-27 and ACT-31 raised Low to
Medium, directed by João the same day doc-43 flagged the question), and one
new test fixture. `git status` was clean at the start of this run.

Two cards closed since doc-43: ACT-60 (m-3, all 8 AC met, reviewed) and ACT-61
(all AC met, reviewed). One card moved column this run: ACT-119, to Shape (see
Classification). Open set: 40 (39 To Do, 1 Shape), unchanged in count from
doc-43 (two closures, one already-counted card reclassified within the open
set).

m-3 now carries one open card: ACT-71. Every other milestone (m-1, m-2, m-5,
m-6, m-7) has all its milestone-tagged work Done except m-1 (ACT-68, ACT-93
still open) and m-6 (ACT-26.7 still open, blocking ACT-27/ACT-31).

## Verified this run

Read every open card. Delegated premise-verification of the 28 cards not
already re-checked by doc-43 (the design-vs-harness inventory cluster ACT-96
through ACT-123, plus ACT-84/95/68/93) to a reviewer sub-agent, then verified
its six findings directly before recording them. All six are citation drift
only (a line number or function name moved since filing); every card's
substantive claim held:

- **ACT-104**: `buildPairedEstimate` is at `comparison-estimator.ts:54`, not
  the cited `:57`.
- **ACT-106**: `assertControlReady`/`assertSourceReady` in `runBenchmark` are
  at `run.ts:857-858`, not the card's implied location. Confirmed the
  triple-check chain live: preflight (`run-command.ts:148`) → `runBenchmark`
  (`run.ts:857-858`) → `confirmRun` (`run-command.ts:328-329`).
- **ACT-112**: `comparison-attribution.test.ts` has five `it()` tests now, not
  four as stated.
- **ACT-116**: the function is `claudeProjectsDirectory`
  (`session-capture.ts:13`), not `defaultProjectsDirectory` as the card names
  it.
- **ACT-118**: the card's cited test line (`context-manifest.test.ts:149`) is
  unrelated; the real `casesRoot()` read is at line 227-228.

Each got a dated note narrowing the citation without touching acceptance
criteria, priority, or status. No other card's claims were found stale.
ACT-107's em-dash claim was independently verified live (present at
`run-history-page.tsx:114` and in `SPEC.md:125`'s empty-state prose), matching
the card exactly. ACT-115's premise (the `--append-notes` fix reachable as an
object but absent from `main` in dotfiles, no rendered skill naming the flag)
was independently re-verified against `~/code/dotfiles` and holds exactly as
recorded.

Confirmed no card is in Build or Review, and no session holds anything.
`git status` was clean before and after this run's card edits (six
`--append-notes` additions plus ACT-119's column move); no acceptance
criteria, priority, or description text was rewritten. The board directory
carries no git history of its own, so nothing here is committed to the
project's repository.

## Classification

**ACT-119** moved To Do → Shape. Its own AC#1 asks whether the four
brief-reply cases keep the population word-band ceiling of 154 or move to
145; that is a decision for João, not something a build session settles on
its own, and the card's own implementation notes say so explicitly ("The
154-to-145 ceiling question is deliberately left open... not this session's
to settle"). Its other two acceptance criteria are conditional on that
answer. Joins the numbered list.

Two similarly-worded cards were checked against the same routing rule and
kept in To Do: ACT-113's "decide whether hashDirectory should refuse a
symlink outright, resolve+contain it via realpath, or something else" and
ACT-104's "settles which quantity the interval represents" are both
implementation choices the builder makes as part of the fix, not decisions
blocked on someone outside the session. ACT-112's AC#1 ("is a deliberate
product decision, not silent first-hash-wins") is approach-neutral and stays
checkable under any of refuse/warn/other, so it also stays a normal build
card.

No overtaken or duplicate card found. The sub-agent checked ACT-96 through
ACT-105 and ACT-112 (all filed from ACT-49's design-vs-harness inventory on
2026-09-07) pairwise, plus ACT-101 against ACT-110 and ACT-102 against
ACT-104 specifically since they sound adjacent: none restates another's
claim, each names a distinct code location and a distinct design-vs-harness
gap. ACT-116/ACT-118 remain correctly cross-referenced as related-but-distinct
(one is capture's session-id resolution scope, the other is a committed
fixture needing a hand-made copy at runtime), matching what both cards
already say.

## Priorities

No priority set or changed this run beyond ACT-119's implicit hold at Medium
through its column move. Priority between features is not triage's to
decide.

## The queue

`backlog task list --ready --sort priority` plus the sitting bundles.

1. **ACT-71** (m-3, Medium): m-3's only open card; closes the milestone to
   5/5 once done.
2. **ACT-114** (m-7, Medium): closes m-7's own named risk, still unproven
   against a real recorded comparison.
3. **ACT-68, ACT-93** (m-1, independent of each other): m-1's last two open
   items.
4. **ACT-84** (Medium, independent).
5. **ACT-69** (blocked on João's budget number).
6. **ACT-27, ACT-31** (Medium since 2026-09-08, blocked on ACT-26.7).
7. **ACT-90, ACT-92** (Low, independent; ACT-92/93 can run together, not
   ACT-90 with either).
8. **ACT-95, ACT-113, ACT-116, ACT-117 (AC#1 only), ACT-118** (Medium,
   independent of each other and the rest).
9. **ACT-115** (Medium): unblocks ACT-86's own pending mechanism. Requires
   editing files agents load as instructions in `~/code/dotfiles`, so it
   goes through `review-instructions` in the same turn as the draft, and the
   commit lands on `main` there, not in this repository.
10. **ACT-106** (independent, unblocked, right-sized; no milestone).
11. **ACT-96-105, ACT-109, ACT-112** (independent backend gaps).
12. **ACT-86** (blocked on ACT-115 landing, then on observing a future
    session's card write).
13. **ACT-83** (parked by direction; do not pick up from the ready queue
    despite showing as ready).
14. **ACT-120** (bug, real cost evidence from a prior run; independent,
    unblocked).
15. **ACT-121, ACT-122** (independent, unblocked).

Bundles that cannot run in parallel: ACT-60/ACT-118's fixture conflict is
resolved now that ACT-60 is Done. Unchanged otherwise from doc-43:
ACT-26.7 before ACT-27/ACT-31; ACT-114's check and ACT-109's schema change
touch the same comparison-record/comparison-report path (confirmed this run
via grep: both reach `comparison-record.ts` and `comparison-report.ts`).

## Kaizen candidates

Doc-43 escalated the claims-without-verification pattern (a card's own
record states a fact that direct inspection contradicts) as a fourth
instance and recommended kaizen start now rather than carry it to a fifth
run. Nothing this run adds a fifth instance; this run's own six findings are
citation drift (a line number moving after the code changed), not a claim
that was false when written, so they are not the same defect class. Not
changing doc-43's recommendation, restating it below since it has not yet
been acted on.

The two other carried candidates (three m-1 reliability cards closed on
inference rather than a live pipeline run; the iteration guard firing on a
session's own assignee bookkeeping) are unchanged and still not started.

## Questions to João

1. **ACT-119** (now Shape): keep the brief-reply word-band ceiling at 154, or
   move it to 145 (the highest length accepted across these four turns)?
   Moving to 145 would additionally fail case `e3dea673`'s already-accepted
   158-word reply under the new ceiling. Recommending: keep 154, since
   lowering it fails a reply João already accepted, and the check is already
   catching the real problem (three of four replies run longer than what was
   accepted, one more than double).
2. **Kaizen**: doc-43's recommendation to start now on the claims-without-
   verification pattern is still open and unactioned. Restating it since a
   fifth run has now passed without it starting.
