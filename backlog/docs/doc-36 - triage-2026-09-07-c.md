---
id: doc-36
title: triage-2026-09-07-c
type: other
created_date: '2026-09-07 20:28'
---

# Triage, 2026-09-07 (third pass, 20:28)

Ninth triage on this board. Previous run: doc-33 (2026-09-07 16:25), 4 hours
earlier. Goal (doc-6/doc-7, unchanged): prove the loop once (m-1), then the
UI three (m-5, m-6, m-7), then m-2, then m-3.

## What changed since doc-33

Two cards closed to Done since doc-33, both with reflections already filed
and read this run: **ACT-49** (design-vs-harness inventory; filed ACT-96
through ACT-105, ten backend-gap cards) and **ACT-88** (preflight halts a
run/replay/calibrate on any missing reference; split off **ACT-106**,
already filed, already right-sized per its own review record). No other
commits landed between doc-33 (16:25) and now. Open set: 18 before, 29 now
(net +11: ACT-96–106 filed, ACT-49 and ACT-88 closed).

## Verified this run

Read doc-33, doc-34 (ACT-49 reflection), doc-35 (ACT-88 reflection), and the
full commit log since doc-33's timestamp: nothing outside the ACT-49/ACT-88
work landed. Cross-checked the board render against both reflections: ACT-49
and ACT-88 are Done as claimed, ACT-96–106 are all present, To Do, and
correctly unprioritized/un-milestoned per their own filing notes. ACT-61
remains in Shape with the same open question doc-28/doc-33 already recorded
answering-in-progress; no change.

Read all ten of ACT-96 through ACT-105 directly: each names a distinct
design-vs-harness gap (corpus-file role, judge evidence source kind, per-step
contribution phrase, culprit-analysis agent, checkpoint id shape, staleness
distance, grade median/range, word-count recording, comparison interval
reading, duration aggregation). No overlap between any pair. ACT-104 is the
only one of the ten that is a direct prerequisite of a milestone card
(ACT-50, feeding m-7's comparison screen, 0/1 done); the rest support ACT-50
more generally or stand free of any milestone card filed so far. This matches
how ACT-49's own card scoped them and is not a defect.

## Classification

No overtaken, duplicate, or stale-premise card found beyond what doc-33
already corrected. ACT-106's premise (duplicate readiness checks) was
verified directly against `src/benchmark/run.ts` and `src/cli/run-command.ts`
by the reflection that split it; not re-verified again here since nothing
touched those files since.

## Priorities

No change. ACT-96 through ACT-106 carry no priority and no milestone, filed
that way deliberately (backend gaps discovered incidentally, not yet
sequenced against the UI milestones they feed). Recommending Medium for
ACT-104 specifically (the only one of the eleven with a direct milestone
dependent, m-7/ACT-50) and leaving the other ten unset until a session
building ACT-50 or the later UI screens confirms which it actually needs
first; setting all eleven now would be guessing at an order the building
session is better placed to find.

## The queue

`backlog task list --ready --sort priority`, ordered against the live goal.
Unchanged in shape from doc-33 except for the new arrivals.

1. **ACT-35** (m-1-adjacent, Medium, ~7 USD, already approved).
2. **ACT-53** (m-5, High, unblocked since ACT-52 and now ACT-49 both Done).
3. **ACT-59** (m-3, High) **→ ACT-60** (m-3, Medium).
4. **ACT-71** (independent).
5. **ACT-84** (Medium, cheap, independent).
6. **ACT-69** (blocked on João's budget number).
7. **ACT-90, ACT-92, ACT-93** (Low, independent; ACT-92/93 can run together).
8. **ACT-95** (Medium, independent).
9. **ACT-106** (independent, unblocked, right-sized; no milestone, does not
   compete with UI work for priority).
10. **ACT-96–105** (ten backend gaps, independent of each other and of the
    rest of the queue; pick up alongside or just ahead of whichever UI
    screen needs the data each supplies).
11. **ACT-86** (still blocked on itself, per doc-33).
12. **ACT-83** (parked).
13. **ACT-27** (Low) **→ ACT-31** (Low), still behind **ACT-26.7** (Low) for
    file overlap.

Bundles that cannot run in parallel: ACT-26.7 before ACT-27/ACT-31 (same
files); ACT-59 before ACT-60 (dependency). Everything else, ACT-96–105
included, touches independent files.

ACT-61 stays excluded from ready: its own open question (declared vs.
discovered project context) is still unanswered.

## Kaizen candidates

No new instance this run. The three carried from doc-33 (a card's own notes
claiming a fix landed that inspection shows was never made, now two
occurrences; three m-1 reliability cards closed on inference rather than a
live pipeline run; the iteration guard firing on a session's own assignee
bookkeeping) are unchanged and still not started. Restating them here would
retell doc-33's record rather than add to it; see doc-33 for the instances
and evidence.

## Questions to João

Doc-28's two questions, restated unanswered in doc-33, are still open;
nothing this run answers them.

1. **ACT-88's scope** was answered directly on the card since doc-33 (all
   three commands, live probe): no longer open, closed with the card.
2. **ACT-61's open question** (declared vs. discovered project context)
   still stands. Doc-28/doc-33's recommendation stands: explicit
   declaration, for attributability.

One new call, not in doc-28/doc-33: should ACT-104 alone get a priority now
(Medium, recommended above) while the other nine of ACT-49's inventory wait
for a building session to sequence them, or should all eleven be prioritized
together in this pass? Recommending the former, since only ACT-104 has a
named milestone dependent today.
