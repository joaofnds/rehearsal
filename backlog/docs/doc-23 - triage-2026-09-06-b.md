---
id: doc-23
title: triage-2026-09-06-b
type: other
created_date: '2026-09-06 12:09'
---

# Triage, 2026-09-06 (second run)

Sixth triage on this board. Previous run: doc-22 (2026-09-06 01:19). The goal
is still doc-6/doc-7's: prove the loop once (m-1), then the UI three (m-5,
m-6, m-7), then m-2, then m-3.

## What changed since doc-22

**ACT-29 closed to Done**, on commits `4cc44b1` through `fa69972`: moved to
Build, fixed (`f549ee0`, removed the leaked scratch directories), reviewed,
closed. m-2 now has only ACT-88 ahead of it (see below).

**ACT-88 filed**, directed by João, split out of ACT-34: halt a run in
preflight when anything it needs (target, task/brief/rubric/pipeline files,
model, budget) is missing. Its own implementation notes end "Open for Shape:
whether preflight is one gate for every command that starts work (run,
replay, calibrate) or only run; and how model availability is checked without
a network call the suite would depend on." That is a question in the card's
own text, so it moves to Shape this run (was To Do).

**ACT-61 reread in full.** Its own text: "Open question this card must
answer rather than assume: whether a case declares its project context
explicitly... or whether the harness discovers it from the target tree."
Same pattern as ACT-88. Moved to Shape this run (was To Do, no priority).

**ACT-86 reread in full and needs no action.** Fully worked this cycle: João
declined the hook mechanism ("no hooks!"), the prose fallback landed in
dotfiles (`e33c7abf`), and the card's own notes already mark criterion #1 as
not yet checkable, pending observation of a future shape or build session
writing a card. Correctly left as an open card carrying its own honest status;
not a defect to fix here.

## Verified this run

- ACT-87's premise verified true directly against current code:
  `list-command.ts:151` still calls `loadRunManifest(paths.manifestFile)` with
  no existence check first.
- ACT-84's acceptance criteria and priority (Medium) unchanged since doc-22;
  no work landed on it this cycle.
- Git status clean; nothing uncommitted at either read.
- No reflection doc newer than doc-22 exists; doc-22 remains the latest input
  this run had to fold in besides the git log.

## Classification

No new overtaken cards this run, no duplicates, nothing else to archive.
ACT-88 and ACT-61 are the only field changes: both are open questions embedded
in work cards, routed to Shape per the board's own rule that a card whose own
text asks a question moves there. Neither needed a split; each is one
question blocking one card.

## The queue

`backlog task list --ready --sort priority`, ordered against the live goal.

1. **ACT-73, ACT-74, ACT-76** (m-1, no priority yet). Unchanged from doc-22:
   all three block trusting the comparison m-1 exists to produce, independent
   of each other.
2. **ACT-63** (m-1, no priority yet). Latent, real, independent of the above
   three.
3. **ACT-68** (m-1, no priority, waits on ACT-28 comment #5).
4. **ACT-88** (Shape now, was m-2's next item). Needs João's answer on scope
   (one gate for run/replay/calibrate, or run only) and on model-availability
   checking before it can be built. Until it's answered, m-2 has nothing
   ready.
5. **ACT-48 → ACT-52 → ACT-53, with ACT-49 alongside** (m-5, unchanged).
6. **ACT-37** (unassigned, waits on ACT-28 comment #5).
7. **ACT-71, ACT-59 → ACT-60** (unassigned; doc-16/doc-22 recommended m-3 for
   these, unanswered, carried forward). **ACT-61** now also waits on its own
   Shape question (declared vs. discovered project context) before it can
   follow ACT-59.
8. **ACT-32 → ACT-35** (m-3), **ACT-84** (no milestone, cheap, real), **ACT-69**
   (blocked on João's budget number), **ACT-83** (parked), **ACT-87** (m-5's own
   gap, per doc-19 recommend Medium, ahead of the unprioritized m-3 cluster,
   behind m-5's screen cards), **ACT-26.7 → ACT-27** and **ACT-31** (low, last).

### Bundles that cannot run in parallel

Unchanged from doc-22: ACT-26.7 before ACT-27/ACT-31 (same files); ACT-32
before ACT-35 (dependency). ACT-73, ACT-74, ACT-76, ACT-63, ACT-68 all touch
different functions and can run in parallel with each other.

## Questions to João

Carried forward from doc-22, unanswered, plus ACT-88's new one:

1. **ACT-73 High, ACT-76 High, ACT-74 Medium, ACT-63 Medium, ACT-68 Low** are
   my recommended priorities; none of the five carries one today. Priority is
   yours.
2. **ACT-28 comment #5** (does local-install corpus-freezing survive a second
   provider) still blocks ACT-37 and ACT-68.
3. **ACT-59, ACT-60, ACT-71** still carry no milestone; recommend m-3 for all
   three (ACT-61 is now in Shape pending its own question, so it's out of this
   batch).
4. **New: ACT-88's scope.** Should preflight validation cover only `run`, or
   also `replay` and `calibrate`? And should model availability be checked
   with a real call, or skipped/stubbed for the test suite? Both block the
   card and, behind it, the rest of m-2.
