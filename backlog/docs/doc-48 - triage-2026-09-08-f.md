---
id: doc-48
title: triage-2026-09-08-f
type: other
created_date: '2026-09-08 21:48'
---

# Triage, 2026-09-08 (f)

Fifteenth triage on this board. Previous run: doc-46 (2026-09-08 20:41). Goal
(doc-6/doc-7, unchanged): prove the loop once (m-1), then the UI three (m-5,
m-6, m-7), then m-2, then m-3.

## What changed since doc-46

One reflection doc landed since doc-46 and outranks it: doc-47 (reflection on
ACT-104, 2026-09-08 21:44). ACT-104 itself closed to Done before doc-46's run
started (doc-46 already recorded that closure), so nothing on the board's
open/closed split moved from this doc. `git log --since` doc-46's timestamp
shows only ACT-104's shaping/build/review commits and its Done reflection,
all already accounted for by doc-46 or read directly this run.

Nothing else changed: no other card moved column, no other decision, no other
reflection newer than doc-46. `git status` was clean at the start of this run
except this run's own new card file.

## Verified this run

Read doc-47 in full and the current ACT-104 card (Done, all 10 AC checked,
build notes and review findings on the card, matching doc-47's account
exactly: the served `qualityReadings` field, the two review-caught bugs
fixed, the client intentionally untouched).

Doc-47 proposes one new card, conditional on checking ACT-50's scope first.
Checked: ACT-50 is Done, and its own text explicitly deferred the "What
moved" tab to ACT-104 by name while shipping Attempt pairs only, so it is not
the right home for new work. Grepped every open and closed card body for
`What moved`, `qualityReadings`, `PlannedFeatureBlock`: only ACT-104, ACT-50,
and ACT-49 (the inventory ACT-104 was filed from) mention any of the three,
and none of the three is open. No existing open card covers wiring the tab.
Checked ACT-114 (m-7, comparison screen never rendered a real record)
against the same gap: distinct work, since ACT-114 is about verifying the
already-built Attempt-pairs UI against a disk record, not about building the
What moved tab at all.

## Classification

**New card filed: ACT-126**, "wire the comparison page's 'What moved' tab to
qualityReadings," per doc-47's proposal exactly, milestone m-7, depends on
ACT-104 (satisfied, Done). No overtaken or duplicate card found this run
beyond what doc-46 already cleared.

ACT-119 checked again against its own routing rule from doc-46: still
correctly in Shape, still waiting on João's word-band ceiling question
(154 vs 145), unchanged since doc-46.

## Priorities

ACT-126 filed at Medium, matching the milestone-m-7 backend cards it follows
(ACT-104, ACT-114) and the UI-completion class of work it is. No other
priority changed this run.

## The queue

`backlog task list --ready --sort priority` plus the sitting bundles.
Unchanged from doc-46 except ACT-126 inserted after ACT-114, since both are
m-7 and ACT-126 is now unblocked (its one dependency, ACT-104, is Done).

1. **ACT-71** (m-3, Medium): m-3's only open card; closes the milestone to
   5/5 once done.
2. **ACT-114** (m-7, Medium): closes m-7's own named risk, still unproven
   against a real recorded comparison.
3. **ACT-126** (m-7, Medium, new): wires the "What moved" tab to the data
   ACT-104 now serves; unblocked.
4. **ACT-68, ACT-93** (m-1, independent of each other): m-1's last two open
   items.
5. **ACT-84** (Medium, independent).
6. **ACT-69** (blocked on João's budget number).
7. **ACT-27, ACT-31** (Medium since 2026-09-08, blocked on ACT-26.7).
8. **ACT-90, ACT-92** (Low, independent; ACT-92/93 can run together, not
   ACT-90 with either).
9. **ACT-95, ACT-113, ACT-116, ACT-117 (AC#1 only), ACT-118** (Medium,
   independent of each other and the rest).
10. **ACT-115** (Medium): unblocks ACT-86's own pending mechanism. Requires
    editing files agents load as instructions in `~/code/dotfiles`, so it
    goes through `review-instructions` in the same turn as the draft, and
    the commit lands on `main` there, not in this repository.
11. **ACT-106** (independent, unblocked, right-sized; no milestone).
12. **ACT-96-105, ACT-109, ACT-112** (independent backend gaps).
13. **ACT-86** (blocked on ACT-115 landing, then on observing a future
    session's card write).
14. **ACT-83** (parked by direction; do not pick up from the ready queue
    despite showing as ready).
15. **ACT-120** (bug, real cost evidence from a prior run; independent,
    unblocked).
16. **ACT-121, ACT-122** (independent, unblocked).

Bundles that cannot run in parallel: unchanged from doc-46. ACT-114's check
and ACT-109's schema change touch the same comparison-record/
comparison-report path. ACT-126 also touches comparison-page.tsx, the same
file ACT-114's browser check reads but does not edit, so ACT-114 and ACT-126
can run in parallel; ACT-126 and ACT-109 do not touch the same files.

## Kaizen candidates

Doc-43's recommendation (start kaizen now on the claims-without-verification
pattern, four-then-five runs unactioned) is unchanged and still open. This
run added no new instance and made no new recommendation. The two other
carried candidates from doc-46 (three m-1 reliability cards closed on
inference; the iteration guard firing on a session's own assignee
bookkeeping) are unchanged and still not started.

## Questions to João

1. **ACT-119** (Shape, carried from doc-43/doc-46): keep the brief-reply
   word-band ceiling at 154, or move it to 145? Recommending: keep 154 (see
   doc-46 for the full reasoning; unchanged this run).
2. **Kaizen**: doc-43's recommendation to start now on the claims-without-
   verification pattern is still open and unactioned across six runs.
